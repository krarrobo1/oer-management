import ResourceList from './contracts/ResourceList.json';
import ResourceTracker from './contracts/ResourceTracker.json';


const options = {
  contracts: [ResourceList, ResourceTracker]
};

export default options;